---
"@gamekits/multiplayer-core": patch
---

Allow authority host loops to publish captured snapshots through a provider-native state writer while preserving the standard tick, diagnostics, and error boundary.
