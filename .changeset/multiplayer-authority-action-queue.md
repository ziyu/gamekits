---
"@gamekits/multiplayer-core": patch
---

Bound host authority action processing with configurable per-source tick and queue limits, expose action queue diagnostics, and reject overflow with a stable error code.
