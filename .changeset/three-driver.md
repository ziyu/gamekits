---
"@gamekits/driver-three": patch
---

Add the initial Three.js driver package with runtime-owned renderer, asset, and camera adapters; object tree support; remote model and texture cache support with load timeouts and compressed glTF decoder setup; material and animation commands; native handles; asset-backed model disposal safeguards; inspect summaries; and diagnostics.
